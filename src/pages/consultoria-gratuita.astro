---
import Layout from "../layouts/Layout.astro";

interface HomeFAQs {
  title: string;
  description: string;
}

const pageTitle = "Nexito Tech: Agenda tu llamada gratuita";
const pageDescription =
  "Agenda tu llamada gratuita y conoce las posibilidades que podemos ofrecerte.";
---

<Layout title={pageTitle} description={pageDescription}>
  <main class="max-w-6xl mx-auto px-4 py-8 min-h-screen">
    <div class="mb-8 text-center">
      <h1 class="text-3xl font-bold text-gray-900 mb-4">{pageTitle}</h1>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">{pageDescription}</p>
    </div>
    <div class="mt-8 grid md:grid-cols-3 gap-6">
      <div class="p-6 bg-white rounded-lg shadow-sm">
        <h3 class="font-semibold text-lg mb-2">✨ Consulta Gratuita</h3>
        <p class="text-gray-600">
          30 minutos para explorar cómo podemos ayudarte con tus objetivos
          tecnológicos.
        </p>
      </div>
      <div class="p-6 bg-white rounded-lg shadow-sm">
        <h3 class="font-semibold text-lg mb-2">🔒 Sin Compromiso</h3>
        <p class="text-gray-600">
          Conversación informal para entender tus necesidades y proponer
          soluciones.
        </p>
      </div>
      <div class="p-6 bg-white rounded-lg shadow-sm">
        <h3 class="font-semibold text-lg mb-2">📅 Flexibilidad</h3>
        <p class="text-gray-600">
          Elige el horario que mejor se adapte a tu agenda.
        </p>
      </div>
    </div>
    <!-- Principio del widget integrado de Calendly -->
    <div
      class="calendly-inline-widget"
      data-url="https://calendly.com/admin-nexito?hide_gdpr_banner=1"
      style="min-width:320px;height:700px;"
    >
    </div>
    <script
      type="text/javascript"
      src="https://assets.calendly.com/assets/external/widget.js"
      async></script>
    <!-- Final del widget integrado de Calendly -->
  </main>
  <style>
    .cal-wrapper {
      border-radius: 0.5rem;
      overflow: hidden;
    }
  </style>
</Layout>
